<?php

namespace App\Actions;

/**
 * Application Action — isolates orchestration from controllers for clean boundaries.
 */
class ProcessOrderAction
{
    public function handle(array $payload): void
    {
        // Placeholder to keep orchestration out of controllers until the pipeline is defined.
    }
}
